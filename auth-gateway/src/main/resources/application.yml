spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/authUsers
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yml
server:
  port: 8080
jwt:
  expiration: 900000  # 15 ????? ? ?????????????
  issuer: auth-gateway
catalog:
  url: http://localhost:8081
logging.level.org.springframework.security: DEBUG